import HrNotifications from '../../../components/notifications/Notifications'

const Notifications: React.FC = () => {
    return (
        <HrNotifications />
    );
};

export default Notifications;
